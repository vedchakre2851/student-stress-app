# Student Stress Prediction App

This is a Streamlit app for predicting student stress levels using a trained machine learning pipeline.

## Features

- Handles preprocessing automatically
- Takes user input through a simple UI
- Predicts stress level instantly

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```
